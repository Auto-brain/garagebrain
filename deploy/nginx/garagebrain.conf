server {
    listen 80;
    server_name garagebrain.yourdomain.com;

    location / {
        root /var/www/garagebrain/frontend/dist;
        try_files $uri $uri/ /index.html;
        expires 1h;
    }

    # Загруженные фото чеков (локальное хранилище VPS, не S3).
    # Должно совпадать с UPLOAD_DIR/UPLOAD_PUBLIC_URL бэкенда.
    location /uploads/ {
        alias /var/www/garagebrain/uploads/;
        expires 30d;
        add_header Cache-Control "public";
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 60s;
        proxy_buffering off;
    }
}

server {
    listen 80;
    server_name gateway.yourdomain.com;

    location /webhook/telegram {
        proxy_pass http://127.0.0.1:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /health {
        proxy_pass http://127.0.0.1:4000;
    }
}
